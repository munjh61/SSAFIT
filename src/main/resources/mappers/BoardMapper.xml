<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.ssafy.ssafit.model.dao.BoardDao">
    <!-- 작성 -->
    <insert id="insertBoard" parameterType="Board">
        INSERT INTO user (user_id, title, content, reg_date, view_cnt, tag)
        VALUES (#{userId}, #{title}, #{content}, #{regDate}, #{viewCnt}, #{tag})
    </insert>

    <!-- 검색 -->
    <select id="selectBoard" parameterType="String" resultType="Board">
        SELECT *
        FROM board
        WHERE board_id = #{boardId} and is_deleted = false
    </select>
    <!-- 상세 조회 -->
    <select id="selectDetailBoard" parameterType="String" resultType="Board">
        SELECT *
        FROM board
        WHERE board_id = #{boardId} and is_deleted = false
    </select>

    <!-- 수정 -->
    <update id="updateBoard" parameterType="Board">
        UPDATE board
        SET title = #{title},
        content = #{content},
        reg_date = #{regDate},
        tag = #{tag}
        WHERE board_id = #{boardId}
    </update>
    <!-- 삭제 -->
    <delete id="deleteBoard" parameterType="String">
        DELETE FROM board
        WHERE board_id = #{boardId}
    </delete>
</mapper>