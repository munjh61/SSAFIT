<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.ssafy.ssafit.model.dao.ImgDao">

    <!--userId가 같은 이미지 리스트 전체 가져오기-->
    <select id="selectImgByUserId" resultType="Img">
        SELECT *
        FROM img
        WHERE board_id = #{boardId}
    </select>

    <!--    <select id="selectImgByUserId" parameterType="String">-->
<!--        SELECT *-->
<!--        FROM img-->
<!--        JOIN board ON img.board_id = board.board_id-->
<!--        WHERE board.user_id = #{userId}-->
<!--    </select>-->
</mapper>